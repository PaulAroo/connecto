import { register_to_dom } from "@benev/slate"

import { MyShadowComponent } from "./components/shadowComponent.js"

console.log("callee")

register_to_dom({ MyShadowComponent })

// define components structure.
// clicks a button to start a call
// highlight webRTCs method of creating a peer
// figure out signalling will be done
// gets a link (has a custom id)
// whoever
// test media capture
// dig into webRTC
